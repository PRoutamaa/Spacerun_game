const gameWidth = 1600.0;
const gameHeight = 1000.0;
const ballRadius = gameHeight * 0.02; 
const shipWidth = 50.0;    
const shipHeight = 50.0;
const shipHorizontalStep = gameWidth * 0.05;   
const shipVerticalStep = gameHeight * 0.05;   